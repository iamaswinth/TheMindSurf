# ===== SYSTEM DEPENDENCIES (Install separately on Windows) =====
# 1. Poppler (for pdf2image): Download from https://github.com/oschwartz10612/poppler-windows/releases/
#    Extract and add the bin/ folder to your PATH
# 2. Tesseract OCR (for pytesseract): Download from https://github.com/UB-Mannheim/tesseract/wiki
#    Install and add to PATH
# 3. libmagic (for python-magic): Install python-magic-bin instead (included below)
# ================================================================

# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Unstructured for document parsing (Python 3.13 compatible)
unstructured[pdf]>=0.18.0
unstructured-inference>=0.7.23

# PDF processing dependencies
pdf2image>=1.17.0
pdfplumber>=0.11.0
pypdf>=3.17.4
pillow>=10.2.0

# OCR support (optional but recommended for scanned PDFs)
pytesseract>=0.3.10

# Additional parsing support
python-magic-bin>=0.4.14; sys_platform == 'win32'
python-magic>=0.4.27; sys_platform != 'win32'
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.2

# Utility libraries
pydantic>=2.5.3
pydantic-settings>=2.1.0

# =============================================================================
# NEW: Multimodal Processing Dependencies
# =============================================================================

# OpenAI for AI-enhanced summaries (GPT-4o vision model)
openai>=1.10.0

# LangChain for document processing and LLM integration
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-core>=0.1.0

# Additional image processing
opencv-python-headless>=4.8.0

# =============================================================================
# Pinecone Vector Database with Integrated Inference
# =============================================================================

# Pinecone client for hybrid search
pinecone>=5.0.0

# Retry logic for Pinecone upserts
backoff>=2.2.0

# Progress bars for batch operations
tqdm>=4.66.0
